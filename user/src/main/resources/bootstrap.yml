spring:
  application:
    name: server-user-config
  profiles:
    active: pro
  cloud:
    nacos:
      config:
        #  配置中心的地址
        server-addr: 47.106.179.74:8848
        #  配置文件的后缀名
        file-extension: yml
        refreshable-dataids: false